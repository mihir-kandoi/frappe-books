{
  "name": "frappe-books",
  "private": true,
  "scripts": {
    "postinstall": "yarn --cwd frontend install --frozen-lockfile",
    "dev": "yarn --cwd frontend dev",
    "build": "yarn --cwd frontend build"
  }
}
