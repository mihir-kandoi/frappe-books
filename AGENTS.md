# Repository scope

Work on `mihir-kandoi/frappe-books`. Push code and open replacement pull requests only in this personal repository.

Treat `frappe/books` as a read-only source unless the user explicitly authorizes a specific GitHub PR action. Do not push code or replacement fixes there.

Preserve uncommitted work from other tasks. Use an isolated checkout when changes overlap.
